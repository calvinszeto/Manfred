<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".HomeActivity$PlaceholderFragment">

    <TextView
        android:id="@+id/saves_screen_header"
        android:text="@string/saves_screen_logo"
        android:textSize="@dimen/title"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_height="wrap_content"
        android:layout_width="fill_parent" />

    <ListView
        android:id="@android:id/list"
        android:layout_height="350dp"
        android:layout_width="match_parent"
        android:layout_below="@+id/saves_screen_header"
        android:layout_marginTop="10dp">
    </ListView>

    <RelativeLayout
        android:id="@+id/new_game"
        android:layout_height="100dp"
        android:layout_width="match_parent"
        android:layout_alignParentBottom="true"
        android:onClick="newGameClicked"
        android:background="@color/button_blue">

        <TextView
            android:id="@+id/start_new_text"
            android:text="@string/start_new"
            android:textSize="28sp"
            android:textStyle="bold"
            android:paddingLeft="10dp"
            android:textColor="@color/black"
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"
            android:layout_centerVertical="true"/>

        <ImageView
            android:id="@+id/manfred_home_picture"
            android:layout_width="180dp"
            android:layout_height="200dp"
            android:src="@drawable/manfred1"
            android:layout_toRightOf="@+id/start_new_text"
            android:layout_centerVertical="true" />

    </RelativeLayout>

</RelativeLayout>